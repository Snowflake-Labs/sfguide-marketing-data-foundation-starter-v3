# Put any commands you would like here to run before the setup script.

## Render the notebook for SQL block support
dataops-render --dir "solution/example-jupyter-notebook" --ext ".template" --overwrite render-dir